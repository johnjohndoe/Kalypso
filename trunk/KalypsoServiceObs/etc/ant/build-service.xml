<?xml version="1.0" encoding="ISO-8859-1"?>
<project name="KalypsoServiceObservation-Services" default="fake" basedir=".">
	<property name="project-name" value="KalypsoServiceObs" />
	<property name="project" value="${workspace}/${project-name}" />
	<property name="projectClient" value="${workspace}/${project-name}Client" />
	<property file="service.properties" />

	<import file="${workspace}/KalypsoServiceCore/etc/ant/service-build.xml" />

	<path id="deploy.classpath">
		<fileset dir="${project.lib}" includes="**/*.jar" />

		<fileset dir="${workspace}/JavaApiContributions/build/deploy" includes="**/*.jar" />
		<fileset dir="${workspace}/KalypsoAuth/build/deploy" includes="**/*.jar" />

		<fileset dir="${workspace}/KalypsoCore/build/deploy" includes="**/*.jar" />
		<fileset dir="${workspace}/KalypsoCommons/build/deploy" includes="**/*.jar" />
		<fileset dir="${workspace}/KalypsoServiceCore/build/deploy" includes="**/*.jar" />
		<fileset dir="${workspace}/KalypsoRepository/build/deploy" includes="**/*.jar" />
		<fileset dir="${workspace}/KalypsoServiceObs/build/deploy" includes="**/*.jar" />
		<!-- This is even more dirty than the rest of this list... but we need the reference to IAdaptable... -->
		<fileset dir="${workspace}/KalypsoBuild/libs4server" includes="**/*.jar" />
	</path>

	<target name="fake" description="fake target, does nothing"/>
</project>
