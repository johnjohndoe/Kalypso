project.src=${project}/src
project.lib=${project}/lib
project.build=${project}/build
project.bin=${project.build}/class
project.deploy=${project.build}/deploy
project.doc=${project.build}/doc

jwsdp=${workspace}/JWSDPInstallation/jwsdp

junit-output=${workspace}/KalypsoBuild/tmp/junit

# these Flags are compile options for javac
flag-debug=true
flag-optimize=true
