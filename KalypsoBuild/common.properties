project.src=${project}/src
project.lib=${project}/lib

# these 2 directories must exist (convention for Kalypso Projects: eclipse compiles to Project/build/bin)
project.build=${project}/build
project.bin=${project.build}/bin

project.deploy=${project.build}/deploy
project.doc=${project.build}/doc

jwsdp=${workspace}/KalypsoCommons
kalypsocommons=${workspace}/KalypsoCommons

# these Flags are compile options for javac and other tasks that compile java code
flag-debug=true
flag-optimize=true
flag-verbose=true