<?xml version="1.0" encoding="ISO-8859-1"?>
<project name="KalypsoMetaDocService" default="fake" basedir=".">

	<property name="project" value="${workspace}/KalypsoMetaDocService" />
	<property name="project-name" value="KalypsoMetaDocService" />
    <property file="service.properties" />

	<import file="${workspace}/KalypsoCoreServices/etc/ant/service-build.xml"/>

	<path id="compile.classpath">
		<fileset dir="${project.lib}" includes="**/*.jar"/>
		<fileset dir="${jwsdp}/jwsdp-shared/lib" includes="**/*.jar" />
		<fileset dir="${workspace}/KalypsoCoreServices/build/deploy" includes="**/*.jar" />
		<fileset dir="${jwsdp}/jaxb/lib" includes="**/*.jar" />
		
		<fileset dir="${workspace}/KalypsoCore/build/deploy" includes="**/*.jar" />
		<fileset dir="${workspace}/KalypsoUtil/build/deploy" includes="**/*.jar" />
	</path>
	
	<!-- Fake target just to allow a default target since ant requires it. Using
         of the parent build target does not work...
	-->
	<target name="fake"/>
</project>
