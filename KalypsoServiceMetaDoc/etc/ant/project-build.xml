<?xml version="1.0" encoding="ISO-8859-1"?>
<project name="KalypsoServiceMetDoc" default="fake" basedir=".">

	<property name="project-name" value="KalypsoServiceMetaDoc" />
	<property name="project" value="${workspace}/${project-name}" />
	<property file="service.properties" />

	<import file="${workspace}/KalypsoBuild/common-project/build.xml" />

	<path id="compile.classpath">
		<fileset dir="${project.lib}" includes="**/*.jar"/>

		<fileset dir="${workspace}/KalypsoServiceCore/build/deploy" includes="**/*.jar" />
	    
		<fileset dir="${workspace}/JavaApiContributions/build/deploy" includes="**/*.jar" />
		<fileset dir="${workspace}/KalypsoCommons/build/deploy" includes="**/*.jar" />
		<fileset dir="${workspace}/KalypsoCore/build/deploy" includes="**/*.jar" />
		<fileset dir="${workspace}/KalypsoMetaDoc/build/deploy" includes="**/*.jar" />
	</path>

	<target name="fake" />
</project>
